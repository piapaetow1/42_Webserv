# 42_Webserv
A custom HTTP/1.1 web server written in C++ (42 project).  Supports GET, POST and DELETE, multiple ports with independent configs,  and a dynamic shopping list frontend with external API integration.
